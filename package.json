{
  "name": "public-resource-map",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "ui"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w ui\"",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w ui",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w backend && npm run typecheck -w ui"
  },
  "devDependencies": {
    "concurrently": "9.2.3"
  }
}
